<% active_table_for :jobs_list_list_table, @tbl, :actions => [['Delete', 'delete', 'Are you sure you want to delete all of these jobs lists and posts?']] do |t| -%>
<tr <%= highlight_row 'jobs_list',t.id  %>>
  <td><%= entry_checkbox 'jobs_list',t.id %></td>
  <td>
    <%= link_to v(t.name), :action => 'index', :path => [ t.id ] %>
  </td>
  <td><%= t.created_at.localize(Configuration.date_format) %></td>
</tr>
<% end -%>
